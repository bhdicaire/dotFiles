# Modified by: Benoît H.Dicaire
# Source: https://starship.rs/config
# https://www.nerdfonts.com/cheat-sheet?set=nf-pl-
# https://gist.github.com/deepanchal/415e78646aaf5e57e10490d01ad90109
# https://starship.rs/config/#prompt

scan_timeout = 40
command_timeout = 2000
# add_newline = true

[palettes.bhd]
# Overwrite existing color
blue = '21'
# Define new color
mustard = '#af8700'

[line_break]
disabled = true

[localip]
disabled = false
ssh_only = true
format = '@[$localipv4](bold red) '

[hostname]
disabled = false
ssh_only = true
format = '[$ssh_symbol](bold blue) on [$hostname](bold red) '
trim_at = '.companyname.com'

[os]
disabled = true
format = "on [($name )]($style)"
#format = "on [($name)($symbol) ($version)]($style)"
style = "bold blue"

[os.symbols]
Alpine = "🏔️ "
Amazon = "🙂 "
Arch = "🎗️ "
CentOS = "💠 "
Debian = "🌀 "
DragonFly = "🐉 "
Fedora = "🎩 "
FreeBSD = "😈 "
Gentoo = "🗜️ "
HardenedBSD = "🛡️ "
Illumos = "🐦 "
Linux = "🐧 "
Macos = " "
Manjaro = "🥭 "
Mint = "🌿 "
NetBSD = "🚩 "
OpenBSD = "🐡 "
openSUSE = "🦎 "
OracleLinux = "🦴 "
Pop = "🍭 "
Raspbian = "🍓 "
Redhat = "🎩 "
RedHatEnterprise = "🎩 "
SUSE = "🦎 "
Ubuntu = "🎯 "
Unknown = "❓ "
Windows = "🪟 "

format = """
[░▒▓](#a3aed2)\
[  ](bg:#a3aed2 fg:#090c0c)\
[](bg:#769ff0 fg:#a3aed2)\
$directory\
[](fg:#769ff0 bg:#394260)\
$git_branch\
$git_status\
[](fg:#394260 bg:#212736)\
$nodejs\
$rust\
$golang\
$php\
[](fg:#212736 bg:#1d2230)\
$time\
[ ](fg:#1d2230)\
\n$character"""

################################################################################
## Custom Commands
################################################################################
[custom.git_server]
#disabled = true
description = "Display symbol for remote Git server"
command = """
GIT_REMOTE=$(command git ls-remote --get-url 2> /dev/null)
if [[ "$GIT_REMOTE" =~ "github" ]]; then
    GIT_REMOTE_SYMBOL=" "
elif [[ "$GIT_REMOTE" =~ "gitlab" ]]; then
    GIT_REMOTE_SYMBOL=" "
elif [[ "$GIT_REMOTE" =~ "bitbucket" ]]; then
    GIT_REMOTE_SYMBOL=" "
elif [[ "$GIT_REMOTE" =~ "git" ]]; then
    GIT_REMOTE_SYMBOL=" "
else
    GIT_REMOTE_SYMBOL=" "
fi
echo "$GIT_REMOTE_SYMBOL "
"""
when = 'git rev-parse --is-inside-work-tree 2> /dev/null'
style = "white"
format = '[$output]($style) '



[character]
success_symbol = "[➜](grey)"
error_symbol = "[➜](bold red)"
vimcmd_symbol = "[❮](bold green)" #The format string used before the text input if the shell is in vim normal mode.
vimcmd_replace_one_symbol= "[❮](bold purple)" #The format string used before the text input if the shell is in vim replace_one mode.
vimcmd_replace_symbol= "[❮](bold purple)" #The format string used before the text input if the shell is in vim replace mode.
vimcmd_visual_symbol= "[❮](bold yellow)"

[directory]
style = "fg:#e3e5e5 bg:#769ff0"
#style = "blue"
format = "[ $path ]($style)"
truncation_length = 20
truncation_symbol = "…/"
truncate_to_repo = false
read_only = " "
home_symbol = " ~"

[directory.substitutions]
"Documents" = " "
"Downloads" = " "
"Music" = " "
"Pictures" = " "
#"/Volumes/Tarmac/Code/w3" = " "
#"/Volumes/Tarmac/Code" = "http://github.com/bhdicaire"

[aws]
disabled = true
symbol = "  "
format = '\[AWS: [$profile/($region)]($style)\]'
style = 'bold orange'

[azure]
disabled = true
symbol = 'ﴃ '
format = 'on [$symbol($subscription)] with [$symbol($username)](($style) '
style = 'blue bold'

[battery]
disabled = true

[buf]
symbol = " "

[bun]
disabled = true
format = 'via [🍔 $version](bold green) '

[c]
symbol = " "

[cmd_duration]
# shows how long the last command took to execute. The module will be shown only if the command took longer than two seconds , or the min_time config value, if it exists
# min_time = 500
min_time = 2000
format = 'underwent [$duration](bold yellow)'

[cobol]
disabled = true

[conda]
format = '[$symbol$environment](dimmed green) '

[container]
format = '[$symbol \[$name\]]($style) '
style = 'bold red dimmed'

[crystal]
disabled = true

[daml]
disabled = true

[dart]
disabled = true
symbol = " "

[docker_context]
symbol = " "

[dotnet]
disabled = true

[elixir]
disabled = true
symbol = " "

[elm]
disabled = true
symbol = " "

[gcloud]
symbol = '️🇬️ '
[gcloud.region_aliases]
us-central1 = 'uc1'
asia-northeast1 = 'an1'

[git_branch]
symbol = ""
format = '[$symbol $branch ]($style)'
style = "fg:#769ff0 bg:#394260"
#ignore_branches = ['master', 'main']

[git_status]
format = '([\[$all_status\]]($style) )'
style = "fg:#769ff0 bg:#394260"
ahead = '⇡${count}'  # Disabled in the prompt format $ahead_behind
behind = '⇣${count}' # Disabled in the prompt format $ahead_behind
conflicted = '🏳'
deleted = '[✘](red) '
diverged = '⇕⇡${ahead_count}⇣${behind_count}'
modified = '[* ](blue)'
renamed = '👅'
staged = '[+$count](green)'
stashed = '📦 '
untracked = ""       # Disabled
#untracked = '🤷 '
up_to_date = '[✓ ](blue)'
# windows_starship = '/mnt/c/Users/username/scoop/apps/starship/current/starship.exe'

[git_commit]
commit_hash_length = 4
tag_symbol = '🔖 '

[git_state]
disabled = true
format = '[\($state( $progress_current of $progress_total)\)]($style) '
cherry_pick = '[🍒 PICKING](bold red)'

[git_metrics]
disabled = true
added_style = 'bold blue'
format = '[+$added]($added_style)/[-$deleted]($deleted_style) '

[golang]
format = '[[ $symbol ($version) ](fg:#769ff0 bg:#212736)]($style)'
# format = 'via [🏎💨 $version](bold cyan) '
symbol = "ﳑ"
style = "bg:#212736"

[guix_shell]
disabled = true
format = 'via [🐂](yellow bold) '

[haskell]
symbol = " "

[haxe]
disabled = true
symbol = "⌘ "

[helm]
format = 'via [⎈ $version](bold white) '

[hg_branch]  # Mercurial
disabled = true
format = 'on [🌱 $branch](bold purple)'
truncation_length = 4
truncation_symbol = ''

[java]
disabled = true
symbol = " "

[jobs]
symbol = '+ '
number_threshold = 2
symbol_threshold = 1

[julia]
disabled = true
symbol = " "

[kubernetes]
disabled = true
detect_files = ['k8s']
format = 'on [⛵ ($user on )($cluster in )$context \($namespace\)](dimmed green) '

[kubernetes.context_aliases]
'dev.local.cluster.k8s' = 'dev'
'.*/openshift-cluster/.*' = 'openshift'
'gke_.*_(?P<var_cluster>[\\w-]+)' = 'gke-$var_cluster'

[kubernetes.user_aliases]
'dev.local.cluster.k8s' = 'dev'
'root/.*' = 'root'

[lua]
format = 'via [🌕 $version](bold blue) '

[memory_usage]
disabled = true
threshold = -1
symbol = ' '
style = 'bold dimmed green'

[meson]
disabled = true
truncation_symbol = '--'
symbol = ' '
style = 'bold dimmed green'

[nim]
disabled = true
symbol = " "

[nix_shell]
disabled = true
impure_msg = '[impure shell](bold red)'
pure_msg = '[pure shell](bold green)'
format = 'via [☃️ $state( \($name\))](bold blue) '

[nodejs]
symbol = " "

[ocaml]
disabled = true
format = 'via [🐪 $version]($style) '

[opa]
format = 'via [⛑️  $version](bold red) '

[openstack]
disabled = true
format = 'on [$symbol$cloud(\($project\))]($style) '
style = 'bold yellow'
symbol = '☁️ '

[package]
format = 'via [🎁 $version](208 bold) '

[perl]
format = 'via [🦪 $version]($style) '

[php]
disabled = true
format = 'via [🔹 $version](147 bold) '

[pulumi] #By default the Pulumi version is not shown, since it takes an order of magnitude longer to load then most plugins (~70ms)
disabled = true
format = '[🛥 ($version )$stack]($style) '

[purescript]
disabled = true
format = 'via [$symbol$version](bold white)'

[python]
format = "[$virtualenv]($style) "
python_binary = 'python3' # Only use the `python3` binary to get the version.
# python_binary = ['./venv/bin/python', 'python', 'python3', 'python2']
# Display the version of python from inside a local venv.
# Note this will only work when the venv is inside the project and it will only
# work in the directory that contains the venv dir but maybe this is ok?

[rlang]
format = 'with [📐 $version](blue bold) '

[raku]
disabled = true
format = 'via [🦪 $version]($style) '

[red]
disabled = true
symbol = '🔴 '

[ruby]
symbol = " "

[rust]
format = 'via [⚙️ $version](red bold)'

[scala]
disabled = true
symbol = " "

[shell]
disabled = true
fish_indicator = ''
powershell_indicator = '_'
unknown_indicator = 'mystery shell'
style = 'cyan bold'

[shlvl]
disabled = false
format = '$shlvl level(s) down'
threshold = 3

[singularity]
disabled = true
format = '[📦 \[$env\]]($style) '

[spack]
disabled = true
format = '[$symbol$environment](dimmed blue) '

[status]
disabled = true
style = 'bg:blue'
symbol = '🔴 '
success_symbol = '🟢 SUCCESS'
format = '[\[$symbol$common_meaning$signal_name$maybe_int\]]($style) '
map_symbol = true

[sudo]
disabled = true
style = 'bold green'
symbol = '👩‍💻 '
allow_windows = true

[swift]
disabled = true
format = 'via [🏎  $version](red bold)'

[terraform] # slow
disabled = true
format = '[🏎💨 $version$workspace]($style) '

[username]
disabled = false
show_always = false
style_user = 'white bold'
style_root = 'black bold'
format = '[$user]($style) '

[vagrant]
format = 'via [⍱ $version](bold white) '

[vlang]
disabled = true
format = 'via [V $version](blue bold) '

[vcsh]
format = '[🆅 $repo](bold blue) '

[zig]
disabled = true
symbol = '⚡️ '


# format = """
# $username\
# $hostname\
# $localip\
# $os\
# $container\
# $shlvl\
# $singularity\
# $kubernetes\
# $directory\
# $vcsh\
# $git_branch\
# $git_commit\
# $git_state\
# $git_status\
# $hg_branch\
# $docker_context\
# $package\
# $c\
# $cmake\
# $elm\
# $golang\
# $haskell\
# $haxe\
# $helm\
# $lua\
# $nim\
# $nodejs\
# $ocaml\
# $opa\
# $perl\
# $php\
# $pulumi\
# $python\
# $rlang\
# $ruby\
# $rust\
# $scala\
# $swift\
# $terraform\
# $vagrant\
# $buf\
# $nix_shell\
# $conda\
# $spack\
# $memory_usage\
# $aws\
# $gcloud\
# $openstack\
# $azure\
# $env_var\
# $sudo\
# $cmd_duration\
# $line_break\
# $jobs\
# $time\
# $status\
# $shell\
# $character"""

