# Used by Git to determine the files and directories to ignore globally.
#
# This list defines global exclusions to all local repositories.
# See https://git-scm.com/docs/gitignore


# ----------------------------------------
# ChezMoi related configuration files
# git itself
!.gitignore
# allow git to see the .config directory
!.config/
# allow git to see the git directory within .config
!.config/git/
# don't ignore anything in this directory
!.config/git/**
# ignore files matching pattern config-*
.config/git/config-*
# ignore global git ignore file to prevent claude from modifying it
.config/git/ignore

# zsh
!.config/zsh/
!.config/zsh/**
.config/zsh/.zsh_sessions
.config/zsh/.zsh_history
# ----------------------------------------

*~
[._]*.un~
Session.vim
Sessionx.vim

# Thumbnails
._*
Thumbs.db

.DS_Store
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent
.AppleDB
.AppleDesktop
Network Trash Folder
Temporary Items
.apdisk
.git/

# ----------------------------------------
# node dependencies
**/node_modules/
npm-debug.log
# ----------------------------------------

*.patch
*.diff

# Ansible
*.retry

## S/MIME message
*.smime

# LibreOffice locks
.~lock.*

# temporary files which can be created if a process still has a handle open of a deleted file
.fuse_hidden*

# KDE directory preferences
.directory

# Linux trash folder which might appear on any partition or disk
.Trash-*

# .nfs files are created when an open file is removed but is still being accessed
.nfs*

# Generated files by hugo
# /public/
/resources/_gen/

# Files best not committed
secring.*
*.ca ## Certificate Authority
*.crt ## Certificate
*.csr ## Certificate Sign Request
*.der ## Certificate
*.kdb ## Key database file
*.org ## OSCP request data
*.p12 ## PKCS #12
*.pem	## PEM-encoded certificate data
*.rnd	## Random number seed
*.ssleay	## SSLeay data

